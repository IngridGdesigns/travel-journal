import React from 'react';

export default function ReadMore(props) {
    return (
        <div>
            <section>
                <h1>{props.item.title}</h1>
            </section>
        </div>
    )
}